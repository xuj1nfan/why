post_install() {
  cat <<'EOF'
why has been installed.

Enable integration for the current shell with one of:
  eval "$(why init bash)"
  eval "$(why init zsh)"

To enable it for future shells, add the matching command to ~/.bashrc or ~/.zshrc.
EOF
}

post_upgrade() {
  post_install
}
